---
id: 017
title: Fix config test failure on main
status: Completed
priority: P1
effort: S
labels: type:bug, area:config
exec_plan: -
depends_on: -
created: 2026-06-15T19:18:44-04:00
updated: 2026-07-06T12:05:45-04:00
---
# Fix config test failure on main

## Problem

The config test at `test/config.test.ts:92` is asserting `strictEqual(true, false)` — a failure on `main`. This breaks CI confidence and masks regressions in the config system.

## Relevant files

- `test/config.test.ts` — the failing test
- `source/config/index.ts` — the module under test

## Implementation notes

1. Run `node --require ./test/setup.js --test test/config.test.ts` to see the exact assertion failure.
2. Determine whether the test expectations have drifted from the actual config behavior, or whether a recent change to the config module broke the contract.
3. Fix whichever side is wrong: update test expectations to match intended behavior, or fix the config module if the test correctly describes the contract.
4. Add a brief comment explaining what the assertion validates so it's easier to maintain.

## Dependencies

None.

## Acceptance Notes

- [x] `node --require ./test/setup.js --test test/config.test.ts` — all 22 tests pass (previously 1 failure).
- [x] `npm run check` — typecheck, lint, format all pass.

The bug was in `source/config/index.ts`: `ensureDefaultConfig()` had an unconditional `catch {}` that swallowed errors from malformed JSON in `_readAppConfig()`. The fix narrows the catch to only handle `ENOENT` (file not found), letting parse errors propagate as exceptions. The test already had a clear assertion message (`"ensureDefaultConfig should throw on malformed JSON"`).

