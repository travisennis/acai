---
id: 014
title: Fix unordered-list markdown regex over-matching inline text
status: Completed
priority: P3
effort: XS
labels: type:bug, area:cli
exec_plan: -
depends_on: -
created: 2026-06-11T20:02:00-04:00
updated: 2026-07-06T20:40:28-04:00
---
# Fix unordered-list markdown regex over-matching inline text

# Fix unordered-list markdown regex over-matching inline text

## Problem

`markdownUnorderedListRegex = /^-\s|\*\s|\+\s/m` parses as `(^-\s) | (\*\s) | (\+\s)`. Only the `-` alternative is anchored to line start; `* ` and `+ ` match anywhere on a line. Verified: inline text like `price is 3 * 4` is misdetected as markdown (returns `true`); the intended `/^(?:-|\*|\+)\s/m` returns `false`.

Confidence: Medium (verified by execution; causes false-positive markdown detection).

## Relevant files

- `source/terminal/markdown-utils.ts` — L7

## Fix direction

Anchor all alternatives to line start: `/^(?:-|\*|\+)\s/m`.

## Acceptance Notes

- `isMarkdown("price is 3 * 4")` returns `false` (was incorrectly `true`).
- Real list lines (`- item`, `* item`, `+ item`) still detected.
- Added unit test covering inline `*` false positive ("should return false for inline text with asterisk operators").
- `npm run check` passes (typecheck, lint, format).
- All 22 tests pass, including the existing list detection tests.

